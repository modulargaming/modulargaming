<h2>
	{{category.title}}
	{{#category.locked}}
		<span class="label label-warning" title="Category is locked">Locked</span>
	{{/category.locked}}
</h2>

{{& pagination}}

<table class="table table-striped">

	<thead>
		<tr>
			<th class="topics">Topics</th>
			<th class="last-post">Last post</th>
			<th class="replies">Replies</th>
			<th class="views">Views</th>
		</tr>
	</thead>

	<tbody>
		{{#topics}}
			<tr>
				<td class="topics">
					<span class="title">
						{{#sticky}}<i class="icon-bullhorn"></i>{{/sticky}}
						{{#locked}}<i class="icon-lock"></i>{{/locked}}
						{{#poll}}<i class="icon-tasks"></i>{{/poll}}
						<a href="{{href}}">{{title}}</a>
					</span>
					<span class="author">
						by <a href="{{user.href}}">{{user.username}}</a> at {{created}}
					</span>
				</td>
				<td class="last-post">
					<span class="author">
						Last post by <a href="{{user.href}}">{{user.username}}</a>
					</span>
					{{created}}
				</td>
				<td class="replies"><span class="badge">0</span></td>
				<td class="views"><span class="badge">0</span></td>
			</tr>
		{{/topics}}
	</tbody>
</table>

{{& pagination}}

{{^topics}}
	<div class="alert alert-error">No forum topics</div>
{{/topics}}

{{#can_create}}
	<div class="form-actions">
		<a href="{{links.create}}" class="btn">New thread</a>
	</div>
{{/can_create}}
